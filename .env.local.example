# Copia este archivo como .env.local (ya está en .gitignore — nunca se sube al repo)
# Reemplaza el valor de DATABASE_URL con tu cadena de conexion real de Neon

DATABASE_URL=postgresql://neondb_owner:TU_PASSWORD@TU_HOST.neon.tech/neondb?sslmode=require
JWT_SECRET=genera-una-clave-aleatoria-de-32-caracteres-aqui
