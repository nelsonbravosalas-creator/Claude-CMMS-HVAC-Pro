/**
 * App.tsx — ya no se usa como punto de entrada principal.
 * El entry point es main.tsx → RouterProvider.
 *
 * Conservado para uso del design system showcase si se necesita.
 * Se puede borrar cuando el proyecto esté en producción.
 */
export default function App() {
  return null;
}
